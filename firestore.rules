rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Denegar todo acceso desde cliente
    // Todas las operaciones se hacen via Cloud Functions con Admin SDK
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
